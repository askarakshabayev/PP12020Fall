#include <iostream>
#include <cmath>

using namespace std;

int main() {
    int n;
    cin >> n;
    cout << sqrt(n) << endl;
    printf("%.1f hello %.4f world %d", sqrt(n), M_PI, n);
    return 0;
}